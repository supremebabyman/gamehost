  - type: web
    name: my-blog
    runtime: static
    buildCommand: npm install
    staticPublishPath: ./
    previews:
      generation: automatic # Enable service previews
    headers:
      - path: /*
        name: X-Frame-Options
        value: sameorigin
    routes:
      - type: redirect
        source: /old
        destination: /new
      - type: rewrite
        source: /a/*
        destination: /a